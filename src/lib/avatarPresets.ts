export const KEYS_MEMOJI_SEEDS = [] as const;

export function buildMemojiAvatarUrl(seed: string): string {
  return "";
}

export const KEYS_AVATAR_PRESETS: string[] = [];

export const DEFAULT_KEYS_AVATAR_URL = "";

export function defaultAvatarUrlForEmail(email: string): string {
  return "";
}
